#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../@ferocia-oss/osnap/bin/osnap" "$@"
else 
  exec node  "$basedir/../@ferocia-oss/osnap/bin/osnap" "$@"
fi
